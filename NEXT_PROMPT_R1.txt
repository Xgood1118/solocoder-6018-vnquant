1. 行业对比排名的结果不对：我把不同行业的股票放一起做财务分析，ROE 排名是所有股票混在一起排的，但需求是按同行业分组排。比如 Banking 和 Technology 的股票排在一起，跨行业比较排名没有意义。
2. CAFE 数据源拉取经常崩溃：旧域名因为 301 重定向后接口返回格式变了，加载到一半直接崩掉，整条数据链走不通。
3. 空数据时对齐函数直接报错：如果某个股票停牌或根本没有数据，数据对齐到面板时直接抛异常后停掉，用户没法决定下一步是跳过去还是怎么处理。
检查修复一下。